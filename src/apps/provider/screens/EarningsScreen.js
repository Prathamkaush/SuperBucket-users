import React, { useCallback, useState } from 'react';
import { Alert, ScrollView, StyleSheet, Text, View } from 'react-native';
import { useFocusEffect } from '@react-navigation/native';
import { getMyJobs } from '../services/provider';
import { Colors, card } from '../theme';
export default function EarningsScreen() { const [jobs, setJobs] = useState([]); useFocusEffect(useCallback(() => { getMyJobs().then((all) => setJobs(all.filter((j) => j.status === 'COMPLETED'))).catch((e) => Alert.alert('Could not load earnings', e.message)); }, [])); const total = jobs.reduce((sum, job) => sum + Number(job.providerEarning), 0); return <ScrollView contentContainerStyle={styles.page}><Text style={styles.title}>Earnings</Text><View style={styles.total}><Text style={styles.totalLabel}>Total earned</Text><Text style={styles.totalValue}>₹{total.toFixed(0)}</Text><Text style={styles.totalHelp}>From {jobs.length} completed jobs</Text></View><Text style={styles.heading}>Completed jobs</Text>{jobs.map((job) => <View key={job.id} style={styles.row}><View><Text style={styles.name}>{job.serviceName}</Text><Text style={styles.date}>{new Date(job.completedAt).toLocaleDateString()}</Text></View><Text style={styles.amount}>+₹{Number(job.providerEarning).toFixed(0)}</Text></View>)}{!jobs.length ? <Text style={styles.empty}>Completed job earnings will appear here.</Text> : null}</ScrollView>; }
const styles = StyleSheet.create({ page: { flexGrow: 1, backgroundColor: Colors.background, paddingTop: 55, padding: 18, paddingBottom: 100 }, title: { fontSize: 25, fontWeight: '900' }, total: { ...card, backgroundColor: Colors.secondary, marginTop: 18 }, totalLabel: { color: '#DCEBFA', fontWeight: '700' }, totalValue: { color: Colors.white, fontWeight: '900', fontSize: 36, marginTop: 8 }, totalHelp: { color: '#DCEBFA', marginTop: 5 }, heading: { fontSize: 18, fontWeight: '900', marginTop: 24, marginBottom: 10 }, row: { ...card, flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center', marginBottom: 8 }, name: { fontWeight: '900' }, date: { color: Colors.muted, fontSize: 12, marginTop: 4 }, amount: { color: Colors.success, fontWeight: '900', fontSize: 17 }, empty: { color: Colors.muted, textAlign: 'center', padding: 30 } });
